<?php
class cache
{
  function load_page($url)
  {
    //checks if page is in cache.  If not cache it and load.
  }
  function clean_up()
  {
    //checks for cache files more than 24 hours old, and removes them.
  }
  function delete(id)
  {
    //removes cache file.
  }
} ?>
